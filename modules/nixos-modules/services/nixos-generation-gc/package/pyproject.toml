[project]
name = "nixos-generation-gc"
description = ""
version = "0.1.0"

[build-system]
requires = ["flit_core>=3.2,<4"]
build-backend = "flit_core.buildapi"

[project.scripts]
nixos-generation-gc = "run:main"
