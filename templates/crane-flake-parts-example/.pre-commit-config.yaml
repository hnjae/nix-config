---
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-toml
  - repo: local
    hooks:
      - id: cargo-fmt
        name: format rust
        entry: cargo fmt --
        language: system
        pass_filenames: true
        types: [rust]
      - id: nixfmt
        name: format nix
        entry: nixfmt --width=100 --
        language: system
        pass_filenames: true
        types: [nix]
      - id: markdownlint
        name: format markdown
        entry: markdownlint-cli2 --fix
        language: system
        types: [markdown]
